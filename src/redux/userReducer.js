const initialStatr = {
  id: 99,
  name: 'Александр Кузиков',
  img: './img/Nd3zIN36rqQ.jpg',
  isAutorizate: true
}

export const userReducer = (state = initialStatr, action) => {
  switch(action.type) {
    default: return state
  }
}